<mvc:View
  controllerName="customdgfiori.controller.Weather"
  xmlns:mvc="sap.ui.core.mvc"
  xmlns:core="sap.ui.core"
  xmlns="sap.m"
  displayBlock="true"
>
  <VBox class="sapUiMediumMargin sapUiResponsiveContentPadding" alignItems="Center">
    <HBox class="sapUiSmallMarginBottom" alignItems="Center">
      <Label text="Şehir Seçin:" labelFor="IDComboCitySelect" />
      <ComboBox
        id="IDComboCitySelect"
        items="{/cities}"
        selectionChange=".onCityChange"
        placeholder="Şehir seçin"
        width="220px"
        editable="true"
      >
        <core:Item key="{lat},{lon}" text="{name}" />
      </ComboBox>
    </HBox>

    <Table
      id="IDTableCurrentWeather"
      items="{weatherModel>/Forecast}"
      inset="false"
      growing="true"
      growingScrollToLoad="true"
      width="100%"
      class="sapUiResponsiveTable"
    >
      <headerToolbar>
        <Toolbar>
          <Title text="📡 Hava Durumu Tahmini" level="H2" />
        </Toolbar>
      </headerToolbar>

      <columns>
        <Column><Text text="📅 Tarih Saat" /></Column>
        <Column><Text text="🌡 Sıcaklık (°C)" /></Column>
        <Column><Text text="🤒 Hissedilen (°C)" /></Column>
        <Column><Text text="🌤 Hava Durumu" /></Column>
        <Column><Text text="💧 Nem (%)" /></Column>
        <Column><Text text="🌬 Rüzgar (m/s)" /></Column>
      </columns>

      <items>
        <ColumnListItem type="Active">
          <cells>
            <Text text="{weatherModel>dt_txt}" class="sapMTextStrong" />
            <ObjectNumber number="{path: 'weatherModel>temp', formatter: '.formatter.formatKelvinToCelsius'}" unit="°C" state="Success" />
            <ObjectNumber number="{path: 'weatherModel>feels_like', formatter: '.formatter.formatKelvinToCelsius'}" unit="°C" state="Warning" />
            <Text text="{weatherModel>weather_desc}" />
            <Text text="{weatherModel>humidity}" />
            <Text text="{weatherModel>wind_speed}" />
          </cells>
        </ColumnListItem>
      </items>
    </Table>
  </VBox>
</mvc:View>
